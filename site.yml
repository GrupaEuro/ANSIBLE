---
- hosts: serwery
  gather_facts: false

  tasks:
    - name: Wyświetl nazwę hosta
      ansible.builtin.command: hostname
      register: wynik

    - name: Pokaż wynik
      ansible.builtin.debug:
        var: wynik.stdout
